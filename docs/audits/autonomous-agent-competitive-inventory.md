# Autonomous Agent Competitive Inventory

This audit uses one rule: every feature must make the autonomous assistant easier or more useful for the user. Internal ownership boundaries matter for implementation and safety, but the user experience should feel like one assistant.

## Sources

- OpenClaw local checkout: `/home/buzzkill/Projects/openclaw`, especially `README.md` and `VISION.md`.
- Hermes Agent current checkout: `/tmp/goodvibes-agent-audit/hermes-agent`, especially `README.md`, `pyproject.toml`, and `docs/observability/README.md`.
- Odysseus current checkout: `/tmp/goodvibes-agent-audit-odysseus/odysseus`, especially `README.md`.
- GoodVibes Agent and GoodVibes host local checkouts: `/home/buzzkill/Projects/goodvibes-agent` and `/home/buzzkill/Projects/goodvibes-tui`.

## Inventory

The source of truth for the structured inventory is `src/agent/competitive-feature-inventory.ts`.

| Feature | OpenClaw | Hermes Agent | Odysseus | GoodVibes now | Target |
|---|---|---|---|---|---|
| One assistant mental model | Gateway is control plane; assistant is the product | CLI, TUI, gateway, and messaging expose one assistant | One web workspace | Leading: summary and visible TUI Home start from the same assistant cockpit lanes while technical boundaries stay in diagnostics | Better |
| First-run and always-on setup | Onboard can install gateway service | Installers and setup wizard configure dependencies and gateway | Docker/native start with admin bootstrap | Partial: visible Start checklist now includes connected-host auth and install smoke beside runtime/model setup; route-backed setup plan exists with local model readiness, probe-fed connected-host diagnostics/status recommendations, token-safe connected-host auth posture with exact pairing route ids, confirmed SDK-backed local token create/repair, inspect-first service install/start/restart routes, offline missing-host bootstrap commands, confirmed token-safe setup smoke execution with durable redacted evidence artifacts, Home/setup latest-smoke plus history/trend surfacing, primary handoff cards for every actionable setup row, and setup-state fixtures | Better |
| Models and local model cookbook | Multi-provider, subscription auth, failover | Many providers and managed tool gateway | Hardware-aware Cookbook for Ollama, llama.cpp, vLLM | Partial: routing, unified readiness scores, hardware-scored local cookbook, setup/download guidance, provider-refresh routes, a visible model-lane local benchmark action backed by `agent_model_compare`, saved local benchmark history, and revealed winner judgments feeding recipe confidence exist; deeper host/server health probes still need depth | Better |
| Omnichannel inbox and delivery | Very broad channel list and DM safety | Telegram, Discord, Slack, WhatsApp, Signal, email | Email, browser, ntfy, PWA | Parity on foundations, needs setup UX polish | Better |
| Email, calendar, notes, tasks | Mail/calendar/reminder workflows in showcase | Email gateway and workspace skills | IMAP/SMTP triage, CalDAV, notes/tasks | Partial: Personal Ops has live note/routine/delivery records, MCP connector setup routes, inbox/calendar connector tool classification, schema-derived inbox/calendar operation records with required fields and confirmation flags, and inbox triage/draft plus calendar agenda/conflict workflow cards; live message/event records still need reviewed provider execution | Better |
| Closed learning loop and personality | Skills and memory | Autonomous memory, skills, session search, user model, custom personality | Persistent vector/keyword memory and skills | Partial: local libraries and prompt injection now require reviewed/confident memory plus reviewed setup-ready skills, routines, bundles, and personas, while suppressed behavior stays visible for curator review; read-only curator with duplicate-consolidation candidates, visible diffs/rollback routes, ordered batch review, confirmed phase helpers with durable receipts, reviewed-note, completed-work, completed-research, and saved-session memory/behavior proposals exists; GoodVibes TUI already has custom system prompt/persona substrate, but Agent still needs a simple VIBE.md personality file and external memory-provider posture; deeper score-based ordering still needs depth | Better |
| Autonomous schedules/background work | Cron, wakeups, webhooks, triggers | Built-in cron with delivery | Scheduled tasks and reminders | Partial: confirmed natural-language autonomous schedule creation is available when task, cadence, success criteria, and user request provenance are explicit; existing schedule edits show read-only current-state diffs before confirmation; read-only ongoing-work intake selects safe routes, including visible webhook/event-trigger setup requests; autonomy queue includes live research runs, connected-host tasks, approvals, automation runs, schedules, tails, task/run diagnostics, source ids, normalized available/unavailable controls, first-class schedule pause/resume aliases, and exact confirmed checkpoint/pause/resume/cancel control routes where supported; richer host output streams still need depth | Better |
| Computer use, browser, shell | Browser, canvas, nodes, system.run | Terminal backends, browser, code execution, computer use, background process controls, sudo support | opencode with web/files/shell/MCP | Partial: local-first execution posture routes read/edit/exec/web/delegation, supervision routes, bounded local execution history records, confirmed file recovery, and strict browser/desktop ready-attention-setup workflow cards; GoodVibes TUI already has background-process, live-tail, shared ProcessManager, PTY-adjacent, and sandbox/sudo substrate; Agent still needs direct user-first process controls and sudo posture where safe | Better |
| Multi-agent and remote execution | Multi-agent routing and session tools | Subagents, kanban, worktrees | Agent runs whole tasks | Partial: GoodVibes has shared-session, remote runner, artifact, task, worktree, subagent, and orchestration foundations; Agent now exposes local-first, TUI handoff, delegated-review, remote-inspection, and hidden-fanout-blocked decision cards plus structured confirmed handoff briefs, but does not yet run cancellable multi-runner work plans from Agent | Better |
| Deep research and reports | Research-oriented workflows | Web tools, session search, trajectory tooling | Deep Research visual reports | Partial: web/URL research, Knowledge ingest, visible local run ledger with log tails, read-only route planning, browser-backed runner readiness with setup/fallback routes, source queue/credibility review, reviewed-source bundles, citation coverage repair hints, and sourced report artifacts exist; browser-backed execution and richer visual reports missing | Better |
| Documents and model comparison | Canvas/web primitives | TUI/dashboard/session tools | Documents and blind Compare | Parity: Document Ops has project-scoped versioned markdown drafts with browse/show/create/revise/review/comment/suggest/accept-suggestion/reject-suggestion/artifact-attach/artifact-insert/export, reviewer-ready export appendices with comment and AI suggestion summaries, unified artifact browse/show/export/package/archive plus confirmed artifact-to-Knowledge promotion, artifact-to-document attachment, and artifact-to-compare reuse for saved text artifacts, and a confirmed blind runner with durable JSON artifacts, saved review boards, side-by-side reviewer views, saved judgments, task/document/benchmark-filtered preference analytics/synthesis, markdown report export, reviewer handoff artifacts, one-click handoff ZIP archives with source evidence, and confirmed route updates | Parity |
| Mobile, voice, device nodes | macOS/iOS/Android nodes and voice | Termux, messaging, voice memo transcription | Responsive PWA | Partial: pairing and voice exist, command depth unfinished | Better |
| Web dashboard and PWA | Control UI and WebChat | Local dashboard | Primary responsive web UI | Partial: host has foundations, Agent is terminal-first | Better |
| Security, permissions, recovery | Secure defaults, pairing, doctor | Approval, isolation, observability | Admin gating and service isolation | Leading: strong policy and audit surfaces | Better |

## Key Findings

1. GoodVibes has many raw foundations already: providers, channels, memory, routines, schedules, MCP, tasks, remote, artifacts, knowledge, media, permissions, and operator APIs.
2. The largest gap is not raw capability count. It is UX integration. Many OpenClaw, Hermes, and Odysseus features already exist in GoodVibes TUI, the connected daemon, or the SDK; Agent should expose those capabilities through user-first routes instead of reimplementing them.
3. The current Agent policy overcorrects against hidden work. The best-in-class target is visible autonomy: jobs can run unattended only when they have clear owner, scope, schedule, status, logs, and cancel/recovery routes.
4. Email, calendar, VIBE.md personality, browser-backed deep research runners, measured local model serving, direct background/PTY process UX, sudo posture, external memory-provider posture, and first-class browser/desktop-control adapters are the clearest product gaps.
5. GoodVibes can lead on safety if it keeps its approval, redaction, trust, readiness, and release-evidence posture while removing unnecessary ceremony from approved workflows.

## Product Direction

- Present one assistant. Summary and visible TUI Home now start with the same assistant lanes; keep package/host ownership language in diagnostics.
- Make setup complete the user's outcome: installed, reachable, authenticated, paired, and ready to act. The current visible Start checklist covers connected-host auth and install smoke alongside runtime/model setup, and setup posture exposes live probe-fed connected-host diagnostics/status recommendations, token-safe connected-host auth posture with exact pairing route ids, confirmed SDK-backed local token create/repair without raw-token output, confirmed service repair routes that stay inspect-first until service status proves need, offline GoodVibes host bootstrap commands before the operator endpoint is reachable, confirmed token-safe setup smoke execution with durable redacted evidence artifacts from package binary to first assistant turn plus Home/setup latest-result and history/trend surfacing, and primary handoff cards from each actionable row to the safest visible form, diagnostic, or confirmed route; guided wizard polish still needs depth.
- Prefer supervised local execution over refusal or delegation friction when the Agent has local permission and workspace context; use `execution_posture` to make that route explicit.
- Use delegation and remote runners for isolation, parallelism, or remote execution, not because the user picked the wrong entrypoint. Keep the handoff brief explicit: original ask, reason, success evidence, workspace/worktree hint, priority, review intent, status route, and recovery path.
- Keep schedule edits explicit, exact-id, and previewed with read-only current-state diffs before confirmation.
- Extend live autonomy queue records with deeper host output streams beyond current task and automation diagnostics when the connected host exposes them.
- Add confirmed webhook and event-trigger creation once the connected host exposes scoped trigger records.
- Execute reviewed email/calendar read routes into live inbox and agenda records, then extend Personal Ops task and reminder queues with provider-backed records.
- Keep learning consolidation explicit: preview duplicate groups, merge survivor fields, stage stale duplicates, delete only after stale, and rollback merge/stale phases through durable receipts.
- Turn research from the current visible run ledger, route planner, and browser-readiness posture into a browser-backed live report workflow with artifacts, cancellation, richer visual reports, and explicit knowledge ingest.
- Add VIBE.md as the primary user-facing personality file, backed by safe discovery, prompt injection, and persona import/review routes.
- Extend Document Ops beyond parity with visual diff-style comparison between reviewer handoff artifacts.
- Add a reviewer-readiness checklist that flags missing source artifacts, unresolved document comments, unrevealed comparisons, and route-change status before export or archive.
- Make browser, mobile, voice, and device control setup discoverable and repairable from the same assistant cockpit.
