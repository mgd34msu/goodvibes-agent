# GoodVibes Platform Capability Audit

This audit records why GoodVibes Agent should use the daemon and SDK as first-class autonomy infrastructure instead of treating them as an external product boundary.

## Local Sources

- `goodvibes-tui` daemon CLI: `/home/buzzkill/Projects/goodvibes-tui/src/daemon/cli.ts`
- GoodVibes TUI operator contract artifact: `/home/buzzkill/Projects/goodvibes-tui/docs/foundation-artifacts/operator-contract.json`
- GoodVibes SDK package: `/home/buzzkill/Projects/goodvibes-sdk`
- SDK operator contract import used by Agent: `@pellux/goodvibes-sdk/contracts`

## Daemon Contract

The current SDK operator contract exposes 279 methods. It includes:

- Automation: job create/list/update/delete/enable/disable/run, run cancel/retry/list/get, schedule create/list/delete/enable/disable/run, heartbeat.
- Sessions and tasks: shared sessions, continuation, task creation, task status, handoffs.
- Channels and delivery: accounts, directories, capabilities, action routes, target resolution, outbound delivery surfaces.
- Knowledge: ask/search/status, URL/bookmark/browser-history/artifact/connector ingest, jobs, reports, refinement, project planning, graph/query routes.
- Media and voice: generation, provider posture, voice routes.
- Remote and services: peer invocation, remote artifacts/review, service status/install/start/stop/restart/uninstall.
- Security and operations: approvals, accounts, auth, telemetry, providers, MCP posture, service/network diagnostics.

## Agent Product Implication

The limiting factor is not raw platform capability. The limiting factor is whether Agent exposes that capability as a coherent user experience:

- The user should see one assistant, not a package boundary.
- Long-running work must be visible, statused, and cancellable.
- Read-only daemon routes should be easy to inspect.
- Write/admin daemon routes should be possible, but confirmation-gated and tied to the user's exact request.
- GoodVibes settings import should remain part of setup so users do not re-enter provider, subscription, permission, UI, and endpoint state.
- The existing terminal renderer should remain the primary shell while Agent gains autonomy-specific controls.

## Current Code Decisions

- `host action:"methods|method"` now fronts the live SDK operator contract inventory; lower-level harness operator modes remain available for compatibility.
- `agent_operator_method` runs exact daemon contract methods with read-only direct execution and write/admin confirmation gates.
- Shared-session follow-up and automation spawn paths now create visible Agent records instead of failing closed.
- The footer reads the active agent read model so autonomous work is visible in the existing renderer.
- Settings visibility no longer hides broad service/control-plane/runtime categories; raw danger toggles stay protected.
- `agent_harness mode:"summary"` and the visible TUI Home view now start with the same assistant-first cockpit for setup, chat/model, project work, Personal Ops, research/docs, background work, and safety/recovery, while host/daemon/provider/MCP/delegation details stay in diagnostics and confirmation boundaries.
- `security action:"explain"` and lower-level `agent_harness mode:"policy_explain"` provide read-only policy explanations for one model action across the Agent route guard, permission mode, and typed-confirmation gate without executing the tool.
- `personal_ops action:"briefing|status|queue|intake|lane|read"` is the preferred model-facing Personal Ops route, backed by the existing harness modes. It builds a read-only daily briefing plan across inbox, agenda, tasks, reminders, routines, delivery, notes, and the autonomy queue before any live personal-data read or effect, exposes a read-only saved inbox/calendar review queue with refresh routes and follow-up boundaries, and turns inbox triage, draft reply, agenda briefing, conflict scan, task, reminder, note, routine, and delivery asks into a preferred Personal Ops lane with exact model route, inspect routes, required or missing fields, next steps, ordered connector-read/local-compose/confirmed-effect execution plans, and confirmation boundary before any connector read, send, schedule, or mutation.
- `personal_ops action:"read"` executes one selected read-only inbox/calendar MCP operation only after required fields, `confirm:true`, and `explicitUserRequest`, refuses write-like connector tools, and returns bounded redacted output plus normalized review cards for local summary or draft review; after the read, structured next-route packets expose lane/queue inspection, confirmed refresh, saved artifact reopen, local draft/reminder work, and separate confirmed send/calendar-edit boundaries. When requested, it saves a durable redacted review-card artifact without full raw connector output or full input values, and saved artifacts resurface as redacted inbox thread/calendar event queue records with artifact inspect routes, freshness status, confirmed refresh routes when a matching read connector is ready, local draft/reminder follow-up routes, and separate confirmed provider-effect boundaries.
- `setup action:"status"` now returns a guided first-run setup wizard plus a prioritized setup plan with connected-host readiness, live service probe evidence, token-safe connected-host auth posture with exact pairing route ids, confirmed SDK-backed local token create/repair through `setup action:"token"`, service status/posture diagnostics, diagnostic/status repair recommendations, setup `serviceLifecycleDecision` gates, inspect-first confirmed service install/start/restart routes with success criteria, certified `agent_operator_method` service receipt outcomes for services.status/install/start/restart, exact install/start/restart/no-action lifecycle decisions from services.status receipts, offline GoodVibes host bootstrap commands for missing-host setup, first-class GoodVibes settings import preview/apply, provider/model access, token-safe install smoke checks, confirmed setup smoke execution through `setup action:"smoke"` with durable redacted evidence artifacts, latest-smoke Home/setup summary surfacing, smoke history/trend/frequent blockers, local model readiness with cookbook follow-through, Agent Knowledge, VIBE.md personality health, local behavior, channels, automation review, sudo execution posture with SUDO_PASSWORD presence-only guidance, delegation, finish state, and primary handoff cards that route every blocked/check/recommended setup row to the safest visible workspace form, diagnostic, user-run guidance, or confirmed route; the visible Start checklist also carries connected-host auth and install smoke next to runtime/model setup. The setup wizard exposes progress, current-step labels, route hints, Agent-owned saved checkpoints with show/save/clear routes, checkpoint auto-advance evidence, backtracking routes, setup-smoke rerun/save routes, and repeated-blocker focus from saved smoke evidence.
- Agent Workspace -> Channels, `/channels guide`, `/channels triage`, `/channels deliveries`, and `channels action:"status|channel|setup|triage|deliveries"` now expose an ordered per-channel setup guide with current channel ranking, setup schema route, secret-backed settings route, delivery target posture, allowlist policy review, live status/doctor checks, explicit confirmed test-send boundary, redacted confirmed-send receipt history without raw webhook/link targets, and read-only triage for setup blockers, daemon delivery attempts, visible surface messages, route bindings, and Agent receipts; lower-level channel harness modes remain compatibility/detail routes.
- `settings action:"list|get|set|reset|import"` fronts Agent settings and the same GoodVibes TUI settings import as the workspace: list/get are read-only, set/reset are confirmation-gated through config/secret managers, import previews by default, and confirmed import copies only Agent-owned settings plus provider subscription state. `import_goodvibes_settings action:"preview|apply"` and `workspace action:"run" actionId:"import-goodvibes-tui-settings"` remain available for compatibility and visible workspace parity.
- Agent Workspace -> Model Routing now exposes separate `Inspect route readiness`, `Local model cookbook`, `Check local servers`, `Run local benchmark`, and `Review benchmark evidence` actions backed by first-class `models action:"status|route|local|providers|provider|smoke"` plus lower-level compatibility routes and `agent_model_compare`, so route posture, provider/subscription posture, local setup guidance, exact local endpoint inspection, confirmed local endpoint smoke execution, smoke-test criteria/triage, live benchmark execution, saved judgments, and default-route apply decisions remain visible and distinct.
- `autonomy action:"intake"` maps ongoing-work requests to the safest visible route and missing fields before any confirmed effect. It now exposes trigger workflow posture and watcher receipt success criteria for time-based wakeups/schedules, incoming webhook/event watchers through the published `watchers.create/list/run/start/stop` operator contract, Gmail/email connector-gated triggers, and read-only control-plane event streams; watcher creation stays an admin confirmed route with trusted source/scope, task/run target, and success criteria required. `agent_operator_method` certifies `watchers.create/patch/run/start/stop/delete` receipts without exposing operator tokens. First-class `schedule action:"list|create|remind|edit|run|pause|resume|delete"` covers connected schedule listing, autonomous schedule creation, reminders, exact-id edits, and lifecycle controls while preserving confirmation boundaries and read-only schedules.list current-state diffs before edit confirmation.
- `autonomy action:"queue|item|status"` now attaches live research run, connected-host task, approval, automation run, and schedule records with exact inspect/checkpoint/pause/resume/cancel/control routes where supported, task retry/output/correlation diagnostics, bounded redacted host task output route/preview descriptors, automation telemetry/delivery/route diagnostics, and schedule pause/resume aliases over daemon enable/disable lifecycle routes. Lower-level `agent_harness mode:"autonomy_intake|autonomy_queue|autonomy_queue_item"` remains available for detailed compatibility inspection.
- First-class `execution`, `terminal`, and `process` adapters expose `execution action:"status|route|history|record|processes|process|recovery"`, `terminal(command, background:true)`, and `process(action:"list|poll|wait|log|kill|write")` UX over the shared SDK/TUI `ProcessManager`, while lower-level execution harness modes remain detailed compatibility surfaces. Together they provide local-vs-delegated route posture, user-visible start/list/status/log/wait/stop routes with bounded redacted output, Work workspace process supervision, process monitor/live-tail handoffs, process-style poll/log/kill/write and session-id aliases, explicit confirmation for start/wait/stop/write, grouped execution-history cards, confirmed file recovery, a read-only process parity matrix for terminal background start plus process list/poll/wait/log/kill/write/PTY/sudo semantics, dynamic SDK/daemon substrate evidence for ProcessManager stdin/PTY methods, terminal/PTY operator routes, session-input routes, and credential routes, confirmed stdin write execution when the shared ProcessManager exposes a safe stdin method, honest PTY unsupported responses until a typed PTY session contract exists, blocked hidden sudo prompts, and a setup-linked sudo posture that reports only credential presence while pointing to foreground-supervised escalation.
- `computer action:"status|control|browser|setup|mcp|open_browser"` now fronts the browser/PWA and browser/desktop-control parts of that execution posture: connected browser cockpit readiness, confirmed visible browser opens, setup/repair rows, and trusted MCP/tool discovery are direct model routes instead of nested harness or device details.
- `device action:"status"` now exposes a read-only companion device capability map backed by pairing token posture, control-plane reachability, the connected browser cockpit/PWA endpoint, voice/TTS readiness, channel/notification readiness, and browser/desktop-control posture. It reports ready, attention, setup-needed, and not-published states so Agent can guide mobile/device UX without claiming camera/location contracts that the SDK/daemon has not published, and the Voice & Media workspace exposes the same map through a Device capability map action. Lower-level `agent_harness mode:"pairing_posture"` remains available for detail.
- `device action:"voice"` now exposes a voice interaction workflow map for push-to-talk input, voice memo transcription, spoken responses, and wake-word capture. Each workflow reports ready, attention, setup-needed, or not-published status, concrete evidence, model routes, setup routes, and policy boundaries so Agent can guide voice setup without pretending always-listening capture is available, and the Voice & Media workspace exposes the workflow map plus Browser/PWA readiness through the direct `computer` route. Lower-level `agent_harness mode:"media_posture"` remains available for detail.
- Documents & Compare now exposes a read-only review packet wizard in the existing renderer and through `agent_harness mode:"document_ops_lane" laneId:"review_packet_wizard"` so draft review, document export, compare judgment, reviewer handoff, route decision, and final archive review have visible progress, current routes, backtracking routes, refreshed-preset lineage verification, and confirmation-gated handoffs.
- Documents & Compare now exposes confirmed Save and Refresh packet preset forms backed by `agent_review_packet_presets`; presets are local JSON artifacts that remember document/export/comparison/judgment/route-decision/handoff/archive/related ids, appear in the packet timeline with attention state when stale, only fill defaults when live packet evidence is missing, list/show freshness checks flag missing or superseded artifact ids with safer reuse routes when newer matching evidence is available, and refresh saves a new local preset artifact with source-preset lineage after confirmation.
- Documents & Compare now exposes a confirmed Share review packet form backed by `agent_review_packet_share`; it starts from a saved reviewer handoff archive, validates archive metadata, previews the delivery target plus packet evidence ids, sends a plain-text archive reference through configured channel targets only after explicit confirmation, and leaves ZIP bytes behind explicit artifact export/package routes.
- `agent_model_compare mode:"apply"` now creates a durable apply-winner route-decision receipt after confirmed model-route updates, while `mode:"routeDecision" decision:"left-unchanged"` records non-mutating route decisions; Document Ops readiness and the packet wizard consume those receipts so route-decision blockers clear before archive evidence exists, and `mode:"handoffArchive"` auto-includes matching receipt artifacts in the handoff ZIP, README, archive metadata, and redacted manifest.
- Runtime prompt composition now applies VIBE.md as personality, then applies separate secret-scanned project context from `.hermes.md`, `HERMES.md`, `AGENTS.md`, `CLAUDE.md`, optional `HERMES_HOME/SOUL.md`, `.cursorrules`, and `.cursor/rules/*.mdc`; `vibe action:"status|show|init|import_persona"` exposes direct personality inspection and confirmed personality setup/import routes, and the Local Context and Personas workspaces show applied/loaded/blocked/truncated health counts and direct inspect actions. The direct `context action:"files|file"` routes expose loaded, truncated, and blocked context files with target-aware subdirectory discovery. Prompt builds now write durable prompt-context receipts with ids, turn/source/model/provider, selected and suppressed record refs, segment counts, prompt hash, size, timestamp, and sanitized terminal outcome for completed/error/cancelled turns without storing raw prompt or response text. `context action:"prompt|receipts|receipt"` exposes applied prompt order, recent receipt ids, exact `receiptId`/`turnId`/`outcomeStatus` filters, turn outcomes, selected VIBE.md/project context/memory/routine/skill/persona records, suppressed records, prompt previews on request, and approximate token budget without mutation; Agent Workspace -> Local Context renders a compact prompt receipt timeline with total/completed/error/cancelled/pending counts, latest outcome, applied/suppressed counts, bounded outcome detail, exact latest-receipt drill-in, and outcome filter routes.
- `agent_harness mode:"agent_orchestration"` and `mode:"agent_orchestration_agent"` now expose live visible Agent subagents, serial-by-default policy, managed multi-agent plan milestones, per-agent plan cards, linked work-plan receipts, closeout routes, remote-runner contract/artifact evidence, auto-attached remote artifact review routes, spawn/batch-spawn decision cards, templates, and exact first-class `agent` inspect/status/budget/plan/wait/message/cancel routes without creating hidden work; approved work-plan items dispatch through confirmed `agent_work_plan action:"dispatch_agents"` and save linked-agent receipts.
- Setup closeout now derives a single user-facing decision from critical setup blockers, durable redacted setup smoke artifacts, and the user onboarding completion marker: `setupWizard.closeout` and top-level `setupCloseout` return blocked, needs-smoke-evidence, ready-to-finish, or complete, while confirmed `setup action:"finish"` writes the same user marker as the visible Finish workspace action.

## Remaining Product Gaps

- Fresh provider-backed thread/event queues and confirmed send/edit/RSVP/archive execution in Personal Ops once connector or daemon records expose durable provider ids; Agent-owned daily briefing, request intake, notes, routines, schedule receipts, delivery channels, email/calendar-capable MCP connector setup routes, expanded connector read/write tool classification, schema-derived operation records with required fields/sample inputs/confirmation flags/fresh-read routes, inbox triage/draft plus calendar agenda/conflict workflow cards, ordered execution plans, confirmed read-only MCP execution with normalized review cards and next-route packets, optional durable redacted review-card artifacts, saved redacted thread/event queue records with freshness and refresh posture, and task/reminder operation cards for visible work plans, connected-host tasks, confirmed reminders, autonomous schedules, and connected schedule controls already surface in live lane records.
- True live host output chunk streams beyond the current bounded task output route/preview descriptors when the connected host exposes them.
- Persisted watcher run history and provider-specific Gmail/email source records beyond the current certified watcher receipts, published `watchers.*` contract, and connector-gated intake.
- Browser/PWA depth beyond the current first-class `computer` route, confirmed connected browser cockpit route, Home and Voice & Media discovery actions, service/web setup fallback, workspace-category coverage map, mobile/PWA control map, and honest Agent-vs-browser first-run receipt reporting; the connected host still needs browser-native Agent workspace category rendering, touch controls, and published browser/PWA completion receipts.
- Mobile/device depth beyond the current companion device capability map, Voice & Media workspace device-readiness action, and voice workflow posture; camera/location sensor contracts, companion-side permission repair, and wake-word capture still need published SDK/daemon records before Agent can offer more than honest setup/not-published guidance.
- Deep research browser-backed execution and browser/PWA report rendering beyond the current visible local run ledger, read-only route planner, run log tails, browser-readiness posture with setup/fallback routes, browser-runner contract map, source queue, credibility scoring, source bundles, visual report packets, citation coverage metadata, repair hints, and saved sourced report artifacts.
- Daemon/SDK-fed local provider health beyond the current endpoint map, exact endpoint inspection, confirmed model-list smoke checks, readiness scoring, hardware-scored cookbook, setup/download guidance, provider-refresh routes, confirmed model-lane benchmark action, saved benchmark-history surfacing, and revealed winner judgments feeding matching recipe confidence.
- Typed PTY process APIs, sudo credential mediation, and true live process-output attachment beyond the current strict browser/desktop ready-attention-setup posture, first-class `computer` adapter, workflow cards/checklists/fallback routes, local-first execution posture for read/edit/exec, setup-linked sudo guidance, tracked background-process routes with process parity/substrate reporting, confirmed stdin-write dispatch when the shared ProcessManager exposes it, web fetch, supervision routes, execution-history activity cards with verification grouping and bounded process-output summaries, confirmed file edit recovery, and first-class delegation routing through `delegation action:"status|routes|route"`.
- Prompt injection now applies secret-scanned project/global VIBE.md personality files, secret-scanned project context files, then uses only reviewed memory at or above the durable confidence threshold plus reviewed setup-ready skills, routines, bundles, and personas; blocked/truncated VIBE.md files, blocked/truncated context files, and unreviewed or setup-blocked behavior are listed as suppressed review work instead of silently steering the assistant. `context action:"prompt|receipts|receipt"` exposes the current selected/suppressed records, recent durable receipt ids, exact receipt/turn/outcome filters, prompt hashes, sanitized turn outcomes, and approximate token budget, while the Local Context workspace shows a compact prompt receipt outcome timeline with exact latest drill-in and outcome filters without raw prompt or response bodies. `memory action:"status|provider"` exposes local memory counts, prompt-active recall, vector stats, embedding-provider doctor warnings, provider inspection, and honest external-memory setup contract maps for Honcho, OpenViking, Mem0, Hindsight, Holographic, RetainDB, ByteRover, and Supermemory while still reporting provider records as not published until the SDK/daemon exposes concrete setup/status/read/write/receipt contracts. `memory action:"curator|candidate"` returns a score-driven prompt plan with prompt-active records, suppressed review/setup/low-confidence/personality/consolidation counts, proposal queues, consolidation queues, ordering rules, and exact review routes; it also ranks local review/setup/stale candidates, VIBE.md personality health cards, duplicate-consolidation candidates with visible diffs and rollback/recreate routes, an ordered duplicate-consolidation batch review plan, first-class confirmed phase helpers with durable receipts, exact-id post-delete recreate guidance, reviewed-note memory/behavior proposals, visible completed-work memory/behavior proposals, completed-research memory/behavior proposals, and saved-session memory/behavior proposals. Profile starter export/import/application can carry VIBE.md with explicit `--include-vibe`.
